side_a = float(input ("what length is side A?"))
side_b = float(input ("what length is side B?"))

area = side_a * side_b
#what length is side A
#what length is side B

print(f"The area is {area:.2f}")
